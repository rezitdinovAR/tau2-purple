# Copy this file to `.env` and fill in your secrets.
# `.env` is gitignored — never commit it.

# === Provider API keys ===
OPENROUTER_API_KEY=

# === Agent behaviour ===
TAU2_AGENT_LLM=
TAU2_AGENT_TEMPERATURE=0.0
TAU2_AGENT_MAX_RETRIES=2
TAU2_AGENT_USE_NATIVE_TOOLS=1